# かめぽす便 仕様書

## 1. 概要

`かめぽす便`は、カメの配達員が日替わりの街を走り、バッグを拾って家へ届けるスマホ向けブラウザゲームです。1プレイは60秒です。ログインなしで遊べること、個人情報を扱わないこと、GitHub Pagesで静的配信できることを前提にしています。

ランキングを有効にする場合だけ、Supabase PostgresとSupabase Edge Functionを使います。フロントエンドから直接DBへ接続せず、公開してよいEdge Function URLだけをHTMLに置きます。

## 2. 公開・実行環境

- 配信方式: GitHub Pagesによる静的配信
- メインURL: `https://24-105.github.io/kameposu/`
- ランキングAPI: `https://aepmulwggolnvnubsvfo.supabase.co/functions/v1/ranking`
- 対象ブラウザ: iOS Safari、Android Chrome、PCブラウザ
- 主対象端末: スマートフォン
- 最低想定画面幅: iPhone SE相当
- ビルド工程: なし
- 外部ライブラリ: フロントエンド側はなし
- サーバーサイド: ランキング有効時のみSupabase Edge Function

GitHub Pagesでは、リポジトリ直下の`index.html`を起点に表示します。zipを解凍した直後に`index.html`、`src/`、`favicon.svg`、`site.webmanifest`などが同じ階層にある必要があります。

## 3. ファイル構成

| パス | 役割 |
| --- | --- |
| `index.html` | アプリ本体のHTML、SEOメタ情報、OGP、JSON-LD、ランキングAPI URL、画面構造 |
| `src/main.js` | ゲームロジック、描画、入力、ランキング通信、結果画面、おすすめ取得 |
| `src/copy.js` | 画面表示文言の管理 |
| `src/styles.css` | スマホ向けUI、レスポンシブ、結果画面、メニュー、アイテム欄のスタイル |
| `favicon.svg` | アプリアイコン |
| `site.webmanifest` | PWA風のメタ情報、ホーム画面追加向け設定 |
| `robots.txt` | 検索エンジン向けのクロール許可 |
| `SPECIFICATION.md` | 本仕様書 |
| `supabase/schema.sql` | ランキング用DBテーブル定義 |
| `supabase/config.toml` | Edge Function設定。`ranking`はJWT検証なしの公開API |
| `supabase/functions/ranking/index.ts` | ランキング取得、スコア投稿、入力検証、CORS制御 |
| `supabase/README.md` | Supabaseセットアップ手順 |
| `.github/workflows/pages.yml` | GitHub ActionsでPagesへ静的ファイルをデプロイする設定 |
| `.gitignore` | zip、env、ログ、node_modulesなどをGit管理から除外 |

## 4. ゲームの目的

プレイヤーはカメ配達員を操作し、60秒以内にできるだけ多くのバッグを家へ届けます。バッグを拾うと黄色線、家へ届けると赤線でルートが表示されます。道具、近道、追い風、ヘリ便などを使ってスコアを伸ばし、今日のランキング上位を目指します。

## 5. 基本ルール

- 制限時間は60秒です。
- バッグを拾うと配達待ちになります。
- バッグを1個以上持って家に到着すると配達完了です。
- バッグは最大3個まで同時に持てます。
- 2個以上まとめて届けると、まとめ便ボーナスが入ります。
- バッグを持っていない時は、最短のバッグへの黄色線を表示します。
- バッグを持っている時は、家への赤線を表示します。
- バッグを3個持っている時は、バッグへのルートは表示しません。
- バッグを0個持っている時は、家へのルートは表示しません。
- 1プレイ終了時に「配達完了」を表示してからリザルト画面へ移動します。
- ランキング、おすすめ、ひと休みなどの右上メニューは、配達完了演出中には表示しません。

## 6. 操作仕様

### 6.1 操作方法

操作方法は2種類あります。

| 操作 | 説明 | 初期設定 |
| --- | --- | --- |
| 画面をなぞる | ゲーム画面上をドラッグしてカメを動かす | デフォルト |
| 下の十字キー | 画面下部の十字キーで上下左右に動かす | 設定で選択 |

選択した操作方法は`localStorage`に保存します。保存キーは`kameposu-control-mode-v1`です。

### 6.2 操作補助

- 小さなドラッグは無視するデッドゾーンを設けています。
- 入力方向がほぼ水平または垂直の場合は、軸方向に寄せて細い道を走りやすくします。
- 移動は短いステップに分割し、壁抜けや急なめり込みを防ぎます。
- 建物の角に引っかかりにくくするため、衝突判定の半径を見た目より小さくしています。
- 建物角は丸みを持たせ、道の中心へ軽く寄せる補助を入れています。
- ターボや追い風中は、交差点や交差点付近で速度補正をかけ、曲がりにくさを抑えます。
- 飛行終了時に道の外へいた場合は、最寄りの着地点へ戻します。

### 6.3 ゲーム中の操作妨害防止

ゲーム領域では、コピー、切り取り、貼り付け、テキスト選択、ドラッグ開始、コンテキストメニューを抑止します。スマホで長押しや誤操作が出てゲーム操作を邪魔しないようにするためです。

## 7. ゲーム状態

| 状態 | 内容 |
| --- | --- |
| `idle` | 初期表示。ルール説明、ランキング、おすすめ、開始ボタンを表示 |
| `countdown` | 開始前カウントダウン。3、2、1、配達へ |
| `running` | ゲームプレイ中 |
| `paused` | ひと休み中。再開、走り直し、ランキング、おすすめ、ルール説明を表示 |
| `finishing` | 終了演出中。「配達完了」を表示 |
| `ended` | リザルト画面表示中 |

「もう一便」を押した時もルール説明を毎回表示します。2回目以降のアクセスでも遊び方を思い出せるようにするためです。

## 8. 日替わり要素

### 8.1 日付キー

日替わり判定はJSTの日付を使います。形式は`YYYY-MM-DD`です。

### 8.2 マップシード

マップは日付キーをハッシュ化した数値から生成します。同じ日なら同じマップになります。ランキング投稿時にも同じ`mapSeed`を送信し、Edge Function側で日付から期待されるシードと一致するか検証します。

### 8.3 今日の街

日付から街の名前を生成します。例として、朝焼け、商店街、水路、駅前通りなどの言葉を組み合わせます。表示例は`今日の街 朝焼けの駅前通り`のようになります。

### 8.4 今日の多めアイテム

日ごとに次のどれかが多めになります。

- 時計多め
- 甲羅守り多め
- 磁石多め
- 追い風多め
- ひこうき多め

## 9. 日替わりミッション

ミッションは10種類あり、日替わりで1つ選ばれます。達成するとゲーム中に一度だけボーナススコアが入ります。

| キー | 内容 | 条件 | 報酬 |
| --- | --- | --- | --- |
| `deliver-3` | バッグを3個、家へ配達しよう | 3個配達 | +430 |
| `deliver-4` | バッグを4個、家へ配達しよう | 4個配達 | +480 |
| `combo-3` | 3連便を決めよう | 3連便 | +470 |
| `carry-2` | バッグを2個、甲羅にのせよう | 2個同時持ち | +430 |
| `items-2` | アイテムを2個集めよう | アイテム2個 | +420 |
| `clock-1` | 時計を1個入手しよう | 時計1個 | +390 |
| `shield-1` | 甲羅守りを1個入手しよう | 甲羅守り1個 | +390 |
| `magnet-1` | 磁石を1個入手しよう | 磁石1個 | +390 |
| `turbo-1` | 追い風を1個入手しよう | 追い風1個 | +390 |
| `flight-1` | ひこうきを1個入手しよう | ひこうき1個 | +390 |

## 10. マップ仕様

- グリッドは38列 x 28行です。
- 1マスは40pxです。
- ワールドサイズは1520px x 1120pxです。
- 道路、建物、川、橋、斜め路地をCanvasで描画します。
- 縦道8本、横道7本を基礎に、枝道、ブロック状の道、斜め路地を追加します。
- 川は横方向または縦方向に1本生成されます。
- 道路と川が重なる場所は橋になります。
- 川は通れますが、橋以外は速度が遅くなります。
- 建物の角には丸みを持たせ、狭い角で詰まりにくくします。
- 斜めに切れたビルや斜め路地を生成し、見た目とルートに変化を出します。

## 11. ルート表示

- バッグへの線は黄色です。
- 家への線は赤です。
- ルートは道路と斜め路地を含めて探索します。
- バッグを持っていない時は、現在位置から最短のバッグ候補へ黄色線を表示します。
- バッグを持っている時は、家への赤線を表示します。
- バッグ所持数が1個または2個の時は、バッグへの黄色線と家への赤線を同時に出します。
- バッグを3個持っている時は、これ以上拾えないためバッグへの黄色線を消します。
- バッグを0個持っている時は、届け先へ行く意味がないため赤線を消します。
- 目的地が画面外にある場合は、画面端の案内で方向を示します。

## 12. プレイヤー

- 主人公はデフォルメされたカメの配達員です。
- バッグを甲羅にのせて配達する世界観です。
- プレイヤー半径は10pxです。
- 通常速度は210です。
- ラスト10秒は少し速くなります。
- 3連便ごとに追い風が発生します。
- 飛行中は道路外を移動できます。

## 13. プレイヤー名

プレイヤー名はアプリが自動生成します。ユーザー入力欄はありません。

- 保存キー: `kameposu-player-profile-v1`
- 形式: 短い言葉A + 接続詞的な短い言葉B + 名詞C
- 文字種: ひらがな、カタカナ、漢字、長音
- 文字数: 2文字から16文字
- 初回生成後は`localStorage`に保存し、ブラウザの保存が残っている限り再使用します。
- タブを閉じても`localStorage`が残っていれば同じ名前を使います。
- ユーザーが名前を入力・編集する仕様はありません。
- サーバーにはこのランダム名だけを送ります。メールアドレス、ユーザーID、端末IDなどは扱いません。

## 14. バッグと配達

- バッグ候補は複数表示されます。
- 初期候補数は、最低表示数5個 + 最大所持数3個を基準にします。
- 配達数やスコアが伸びると、バッグ候補が増えます。
- バッグは最大3個まで持てます。
- バッグを拾うと+25点に、距離に応じた候補ボーナスが加わります。
- 家へ届けると、基本点、距離ボーナス、連便ボーナス、早便ボーナス、まとめ便ボーナスが入ります。
- 1個でも届ければ配達完了としてカウントします。

## 15. スコア仕様

スコアは0未満になりません。

| 種別 | 内容 |
| --- | --- |
| バッグ取得 | `25 + バッグ候補ボーナス` |
| 通常配達 | `130 x 配達個数` |
| 距離ボーナス | 持っていたバッグの距離合計に応じて加点 |
| 連便ボーナス | 連続配達数に応じて加点 |
| 早便ボーナス | 目標秒数より早く届けると加点 |
| まとめ便ボーナス | 2個以上まとめて届けると加点 |
| 時計ボーナス | 時計取得でスコアと時間を加算 |
| 星 | +120 |
| 追い風マス | +45 |
| 下り坂 | +28 |
| ワープ | +30 |
| 屋台 | +35 |
| ポスト便 | 配達1個をイベント配達。基本+88 |
| ヘリ便 | 配達1個をイベント配達。基本+105 |
| すれすれ回避 | +12 |
| 工事接触 | -45 |
| 電気 | -55 |
| 踏切 | -50 |
| 赤信号 | -35 |
| 祭り列 | -25 |
| 車などの移動障害物 | -60 |

スコア内訳は次のバケットで保持します。

- `pickup`
- `delivery`
- `distance`
- `combo`
- `fast`
- `multi`
- `bonus`
- `event`
- `near`
- `penalty`

## 16. 手動アイテム

ゲーム画面下部のアイテム欄に表示します。所持していないアイテムも表示し、使えるアイテムは見た目で分かるようにします。

| アイテム | 初期所持 | 効果 |
| --- | --- | --- |
| 時計 | 0 | 使うと時間を+4.0秒 |
| 甲羅守り | 1 | 6秒間、1回だけ接触を防ぐ |
| 磁石 | 1 | 7秒間、バッグ、家、アイテムの取得範囲を広げる |
| 追い風 | 1 | 2.1秒間、速度が上がる |
| ひこうき | 0 | 3.8秒間、道をひとっ飛びできる |

ゲーム中、残り10秒以下で時計を持っている時、危険の近くで甲羅守りを持っている時など、使うとよいアイテムを強調表示します。

## 17. 拾えるアイテム

マップ上に18個配置します。今日の多めアイテムに応じて出現配分が少し変わります。

| アイテム | 取得時の効果 |
| --- | --- |
| 時計 | すぐ時間を+4.0秒、スコア加点 |
| 甲羅守り | 手動アイテムとして1個追加 |
| 磁石 | 手動アイテムとして1個追加 |
| 追い風 | 手動アイテムとして1個追加 |
| ひこうき | 手動アイテムとして1個追加 |
| 星 | スコア加点 |

各アイテムの説明表示は、1プレイ中にアイテム種別ごとに最初の1回だけ表示します。時計を初めて拾った時に時計の説明を出し、2回目の時計では出しません。磁石、甲羅守り、追い風、ひこうき、星も同じ扱いです。

## 18. マップイベント・障害物

### 18.1 有利なイベント

| イベント | 内容 |
| --- | --- |
| 追い風 | 触れると短時間速くなり、+45 |
| 下り坂 | 坂の向きに沿うと短時間速くなり、+28 |
| ワープ | ペア地点へ移動し、+30 |
| 屋台 | ランダムなアイテムを獲得し、+35 |
| ポスト便 | バッグを持っている時に触れると1個を配達 |
| ヘリ便 | バッグを持っている時に触れると1個を家へ届ける |

### 18.2 妨害イベント

| イベント | 内容 |
| --- | --- |
| 水たまり | 短時間減速 |
| 人混み | 短時間減速 |
| 油 | すべって操作しづらくなり、連便が減る |
| 電気 | 一定時間停止、連便リセット、減点 |
| 踏切 | 閉じている時に触れると減点、時間減少、連便リセット |
| 赤信号 | 赤の時に触れると減点、時間減少、連便リセット |
| 祭り列 | 混雑中に触れると減速、減点、連便が減る |
| 工事 | 接触で減点、時間減少、連便リセット |

### 18.3 移動障害物

道路上を移動する障害物があります。

- 車
- バス
- スクーター
- スケーター
- 歩行者

接触すると減点、時間減少、連便リセットが入ります。接触直後は短い無敵時間を付け、同じ障害物に連続で当たりすぎないようにします。

## 19. 川と橋

- 川は日替わりで縦または横に生成します。
- 橋は通常速度で通れます。
- 橋以外の川は通れますが、速度が0.58倍になります。
- 川に入ると「川はゆっくり」と表示し、仕様として遅い道であることを伝えます。

## 20. チュートリアル・ルール説明

開始前と一時停止中にルール説明を表示します。

- バッグを甲羅にのせて家へ配達する目的を表示します。
- 黄色線はバッグへの道、赤線は家への道であることを表示します。
- 画面をなぞってカメを動かす簡単なアニメーションを表示します。
- 操作方法の切り替えを表示します。
- 文章を増やしすぎず、見本アニメーションで理解しやすくします。

## 21. UI/UX仕様

- スマホ特化です。
- iPhone SE相当の幅でも操作できることを重視します。
- 画面上部にタイトル、今日の街、メニュー、開始・ひと休みボタンを置きます。
- HUDは時間、スコア、お届け数、連便を表示します。
- ゲーム画面下に、手動アイテム欄を置きます。
- 十字キー選択時でもランキングやおすすめがスクロールで見えるようにします。
- リザルト画面は、概要、ランキング、詳細を分けて表示量を抑えます。
- 「詳細を閉じる」ボタンは、詳細内の枠と見間違えにくい見た目にします。
- ランキングとおすすめボタンは、アイコンと文字の位置を揃えます。
- 背景は目がチカチカしないよう、繰り返し模様を強くしすぎません。
- カメ配達員の世界観に合う文言を使います。

## 22. パフォーマンス仕様

低性能なスマホでもなるべく滑らかに動くよう、次を行います。

- 画面外のマップ描画はできるだけ避けます。
- パーティクル数に上限を設けます。
- 低性能端末ではCanvasの描画解像度を少し抑えます。
- `requestAnimationFrame`は、`idle`、`ended`、`paused`で無駄に回し続けません。
- HUDやアイテム欄は、値が変わった時だけDOMを書き換えます。
- 配列は毎フレーム作り直しすぎず、使い回しながら更新します。

## 23. リザルト仕様

リザルト画面では次を表示します。

- ランクバッジ
- カメ配達員名
- 今日のかめ便の達成状況
- 今日の順位
- スコア
- 1位や上位との差分
- もう一便ボタン
- 次回の狙い
- 配達メモ
- 詳細
- ランキングタブ
- おすすめゲーム

詳細には次を表示します。

- この端末の最高スコア
- 自己ベストまで、または更新差分
- スコア内訳
- 配達数
- 最大連便
- 各アイテム取得数
- ぶつかった数
- すれすれ回避
- 総配達数
- 遊んだ回数

不要な称号文や過剰な「ありがとう」メッセージは出しません。ランクバッジは残します。
- 自己ベストを更新した場合は、「自己ベストまで」は表示しません。
- 1位になった場合は、「1位との差」は表示しません。
- 自己ベストと同点の場合も、「自己ベストまで」は表示しません。
- ランキング1位の記録には「1位との差」を表示しません。

## 24. ランク判定

| ランク | 条件 |
| --- | --- |
| SS | 12件以上配達、かつ接触0 |
| S | スコア6800以上、または11件以上配達 |
| A | スコア4800以上、または8件以上配達 |
| B | スコア2800以上、または5件以上配達 |
| C | 1件以上配達 |
| D | 0件配達 |

## 25. ランキング仕様

### 24.1 表示

- ホーム画面からゲーム開始前にランキングを開けます。
- リザルト画面からもランキングを開けます。
- 表記は「ランキング」に統一します。
- 今日のランキングはベスト10まで表示します。
- 7日分のデイリーチャンピオンは、今日を含む直近7日それぞれの1位だけ表示します。
- 7日分の表示は、今日を含めた直近7日分です。
- ランキングが空の場合は「まだ記録なし」と表示します。
- ホーム画面から開いたランキングでも、自分の記録があれば強調表示します。
- リザルト画面では今回の記録を強調表示します。
- 同名が並ぶことは許容します。

### 24.2 順位

- スコアが高い順です。
- 同スコアの場合は投稿が早い順です。
- 同じ投稿がローカルとオンラインの両方にある場合は重複表示しません。
- リザルト画面では、投稿直後にオンラインランキングが返ってきても同じ記録が2位、3位などに重複しないようにします。
- 1位の記録は、差分表示の対象にしません。
- 自己ベスト更新時も、差分表示の対象にしません。

### 25.3 ローカル記録

- 保存キー: `delivery-panic-session-scores-v1`
- `localStorage`と`sessionStorage`を併用します。
- 最大80件まで保持します。
- 保存に失敗してもゲームプレイは止めません。

### 25.4 オンラインランキング

ランキングAPI URLが設定されている場合、Edge FunctionにGET/POSTします。設定が空ならローカル記録だけで動きます。

GET:

```txt
GET /functions/v1/ranking?date=YYYY-MM-DD
```

POST:

```json
{
  "dateKey": "2026-05-31",
  "randomName": "ぽすぽすのカメ便",
  "score": 1234,
  "mapSeed": 123456789,
  "deliveries": 5,
  "combo": 3,
  "collisions": 1,
  "nearMisses": 2,
  "itemPickups": {
    "clock": 1,
    "shield": 0,
    "magnet": 1,
    "turbo": 1,
    "flight": 0,
    "star": 2
  },
  "scoreBreakdown": {
    "pickup": 100,
    "delivery": 650,
    "distance": 220,
    "combo": 105,
    "fast": 90,
    "multi": 0,
    "bonus": 160,
    "event": 45,
    "near": 24,
    "penalty": -60
  }
}
```

## 26. Supabase DB仕様

テーブル名は`public.scores`です。

| カラム | 型 | 内容 |
| --- | --- | --- |
| `id` | bigint identity | 主キー |
| `date_key` | date | JST日付 |
| `random_name` | text | 自動生成プレイヤー名 |
| `score` | integer | スコア |
| `map_seed` | bigint | 日替わりマップシード |
| `deliveries` | integer | 配達数 |
| `combo` | integer | 最大連便 |
| `collisions` | integer | 接触数 |
| `near_misses` | integer | すれすれ回避数 |
| `item_pickups` | jsonb | アイテム取得数 |
| `score_breakdown` | jsonb | スコア内訳 |
| `created_at` | timestamptz | 作成日時 |

DBにはユーザーID、IPアドレス、User-Agent、端末ID、ブラウザ指紋、メールアドレスなどを保存しません。

RLSは有効化します。`anon`と`authenticated`からの直接アクセスは取り消し、`service_role`だけに`select`と`insert`を許可します。フロントエンドはDBへ直接アクセスしません。

## 26. Edge Function仕様

### 26.1 環境変数

| 変数 | 内容 |
| --- | --- |
| `KAMEPOSU_DATABASE_URL` | SupabaseプロジェクトURL |
| `KAMEPOSU_SERVICE_ROLE_KEY` | service role key。フロントへ絶対に出さない |
| `APP_ORIGINS` | 許可するOrigin。カンマ区切り |

### 26.2 CORS

- 許可Originは`APP_ORIGINS`で制御します。
- 未設定または`*`なら全Origin許可です。
- 許可されていないOriginは403を返します。
- `OPTIONS`には204を返します。

### 26.3 入力検証

Edge Function側で次を検証します。

- 日付形式が`YYYY-MM-DD`であること
- 投稿日付がJSTの今日から大きくズレていないこと
- ランダム名が2文字から16文字で、ひらがな、カタカナ、漢字、長音のみであること
- スコア、配達数、連便、接触数などが範囲内であること
- `combo`が`deliveries`を超えないこと
- `mapSeed`が日付から計算した値と一致すること
- アイテム取得数の合計が上限を超えないこと
- スコア内訳が許可されたキーの範囲内であること

### 26.4 レスポンス

GET/POSTともに次の構造を返します。

```json
{
  "dateKey": "2026-05-31",
  "today": [],
  "week": [
    {
      "dateKey": "2026-05-25",
      "top": null
    }
  ]
}
```

POST成功時は、保存した`record`と更新後の`ranking`を返します。

## 27. 個人情報・プライバシー方針

このアプリでは個人情報を扱いません。

保存しないもの:

- ユーザーID
- ログイン情報
- メールアドレス
- 氏名
- IPアドレス
- User-Agent
- 端末ID
- ブラウザ指紋
- 位置情報
- 操作ログ

ブラウザ内に保存するもの:

- 自動生成プレイヤー名
- この端末のスコア記録
- 累計配達数
- 遊んだ回数
- 操作方法の選択

サーバーに送るもの:

- 自動生成プレイヤー名
- スコア
- 配達数
- 最大連便
- 接触数
- すれすれ回避数
- アイテム取得数
- スコア内訳
- 日付
- マップシード

操作ログは送信・保存しません。

## 28. おすすめゲーム仕様

おすすめには次のゲームを載せます。

- `https://24-105.github.io/classic-single-obstacle-ride/`
- `https://24-105.github.io/machi-narabe/`
- `https://24-105.github.io/hitoyo-saisyucho/`

おすすめカードは、可能な限りリンク先ページから次を取得します。

- favicon
- タイトル
- 説明
- JSON-LDの`genre`の最初の1要素

取得できない場合は、適当な文言を補完しません。404や取得失敗時は、無画像と空文字表示にします。

自分自身のページにも他アプリから参照しやすいように、次を用意しています。

- `application-name`
- `apple-mobile-web-app-title`
- `description`
- OGP
- Twitter Card
- JSON-LD `VideoGame`
- JSON-LD `genre`
- favicon
- manifest

## 29. SEO・共有仕様

`index.html`に次を設定します。

- `title`
- `description`
- `keywords`
- `canonical`
- OGP
- Twitter Card
- JSON-LD `VideoGame`
- favicon
- web manifest
- `robots.txt`

リザルトで自己ベストを更新した場合、Web Share APIまたはクリップボードを使って結果を共有できます。画像保存ではCanvasで縦長の結果画像を生成します。

## 30. セキュリティ方針

- フロントエンドにSupabase service role keyを置きません。
- フロントエンドに置くのは公開用Edge Function URLだけです。
- DBはRLSを有効にします。
- `anon`と`authenticated`からscoresテーブルへの直接アクセスを取り消します。
- Edge Functionで入力値を正規化・検証します。
- スコアの完全な不正対策は行いませんが、異常値、日付ズレ、mapSeed不一致は拒否します。
- 操作ログ検証は行いません。
- エラー時もゲーム本体は止めず、ローカル記録で遊べるようにします。

## 31. GitHub Pages配布仕様

zipには、GitHub Pagesで静的配信するために必要なファイルだけを入れます。

含めるもの:

- `index.html`
- `favicon.svg`
- `site.webmanifest`
- `robots.txt`
- `src/`
- `supabase/`
- `.github/workflows/pages.yml`
- `.gitignore`
- `SPECIFICATION.md`

含めないもの:

- `.git/`
- `.DS_Store`
- `.env`
- `.env.*`
- `node_modules/`
- `dist/`
- `.supabase/`
- `supabase/.temp/`
- `*.zip`
- `*.log`
- 動画ファイル
- プロモーション生成物

## 32. GitHub Pages公開手順

1. zipを解凍します。
2. 解凍後の中身をGitHub Pages対象リポジトリのルートへ置きます。
3. `index.html`がリポジトリ直下にあることを確認します。
4. GitHub Pagesの公開元を設定します。
5. GitHub Actionsを使う場合は、`Publish static game`ワークフローを手動実行します。
6. ランキングを使う場合は、Supabaseで`schema.sql`を実行し、Edge Functionをデプロイします。
7. Edge Function URLが`index.html`の`kameposu-ranking-api`に入っていることを確認します。

## 33. カスタムドメインを使う場合

現在のメタ情報と共有URLは`https://24-105.github.io/kameposu/`を基準にしています。`kameposu.jp`などの独自ドメインを正式に使う場合は、次を合わせて更新します。

- `index.html`の`canonical`
- `index.html`の`og:url`
- `src/copy.js`の`share.url`
- JSON-LDの`url`
- JSON-LDの`image`
- `APP_ORIGINS`
- 必要に応じてGitHub Pagesの`CNAME`

## 34. 既知の制約

- ランキングはログインなしなので、同一人物の厳密な本人確認はしません。
- 同名の別プレイヤーは許容します。
- スコア改ざんの完全防止はしません。
- ランキングAPIに届かない場合はローカル記録だけで表示します。
- 外部おすすめページのメタ情報は、相手ページの公開状態やCORSにより取得できない場合があります。
- ブラウザのストレージが無効な場合、名前や記録の永続化はできません。
